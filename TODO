# TODO

- Robot class, getting all the faces from FACES.ini (with different emoji), set default faces, and switch emotions, switch emoji

## config : configparser
- general : config.ini for token api and simple user+password
- https://github.com/theskumar/python-dotenv#readme

## db : sqlite
- used by desktop-app and web-app

## web-app : flask 
- Configurer le config.ini depuis app flask (token openai, password, access db, etc..)

## desktop-app : pygame
- get the camera stream from camera module and receive validation if a student is connected.
- for dev: buttons to access and work individually with each modules

## camera : 
* upload image json from a flask route ?

# matos et shop :
* achat clé token 42 api ?
* raspberry pi ?
* camera, matrix led screen, micro, speaker ?

## Raspberry pi
- Script qui instal les paquet pour une fresh install du raspberry pi
- Script qui lance le serveur flask et l'interface ecran sur le raspberry pi
